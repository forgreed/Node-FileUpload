doctype html
html
    head
        title Upload Example
    body
        h1 Upload a file
        form#uploader(enctype='multipart/form-data', action='/', method='post')
            input#file(type='file', name='file')
            input#submit(type='submit', value='upload')